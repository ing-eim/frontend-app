export const environment = {
  production: false,
  //apiUrl: 'http://127.0.0.1:8000' // Cambia esta URL por la del servidor en producción
  apiUrl: 'http://dwh.api.retornologistico.com' // Cambia esta URL por la del servidor en producción
};
